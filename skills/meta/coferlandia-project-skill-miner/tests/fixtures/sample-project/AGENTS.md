# Sample Project AGENTS

Read `RUNBOOK.md` before touching operations.

- Local verification is safe without approval.
- Anything that reaches production data needs explicit approval.
- Historical notes under `docs/legacy/` are not authoritative unless current docs
  point back to them.
