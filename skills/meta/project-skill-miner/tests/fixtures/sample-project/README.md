# Sample Service

## Local setup

1. Create a virtual environment.
2. Install dependencies with `pip install -r requirements.txt`.
3. Run `python -m pytest tests/test_smoke.py`.

Use this sequence before commits and before changing ingestion logic.

## Diagnostics

For recurring provider ingestion issues, see `RUNBOOK.md`.
